
<div class="card card-primary">
  <div class="card-header">
    <h1 class="card-title"><%=action_message%></h1>
  </div>
</div>

<%if @subject.errors.any?%>
  <div class="alert alert-danger alert-dismissable">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
    <ul>
      <% @subject.errors.full_messages.each do |message|%>
       <li>
         <%= message%>      
      </li>
      <%end%>
     
    </ul>

  </div>
<%end%>


  <!-- /.card-header -->
  <!-- form start -->
  
  <%= form_with(model: [ :admins_backoffice, @subject ], local: true) do |form|%>
       <div class="card-body">
        <div class="form-group">          
          <%=form.label :description%>
          <%=form.text_field :description, class:"form-control",placeholder:t('placeholders.description')%>
        </div> 
        
        <div class="card-footer">
          <%=form.submit t('buttons.submit'), class:"btn btn-primary"%>

        </div>   
      
  <%end%>
